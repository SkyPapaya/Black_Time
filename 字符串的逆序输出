#include<stdio.h>
#include<string.h>
int main() {

	char ch1[100];
	gets(ch1);
	int count = 0;
	
	for (int i = 0; i < 100; i++) {
		if (ch1[i] > 1) {
			count++;
		}
	}
	for (int k = count; k--; k >= 0) {
		printf("%c", ch1[k]);
	}

	return 0;

}
